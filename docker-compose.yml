services:
  mongodb:
    image: mongo:7
    container_name: bornblix_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: bornblix

volumes:
  mongo_data:
